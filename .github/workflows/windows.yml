name: windows

on:
  push:
  pull_request:
    branches: [master]

jobs:
  build-and-test:
    name: Build and test ${{matrix.build-type}} mode
    runs-on: windows-2022
    strategy:
      matrix:
        build-type: [Debug, Release]

    steps:
      - name: Clone recursively
        uses: actions/checkout@v4
        with:
          repository: mimir/mimir
          path: mimir
          submodules: recursive

      - name: Clone mlir plugin
        uses: actions/checkout@v4
        with:
          path: mimir/extra/mlir

      - name: Patch lit.cfg.py to remove FileCheck quoting
        shell: pwsh
        run: |
           $content = Get-Content -Raw mimir/lit/lit.cfg.py
           $old = "config.substitutions.append(('%FileCheck', '""{}""'.format(config.filecheck)))"
           $new = "config.substitutions.append(('%FileCheck', config.filecheck))"
           $content = $content.Replace($old, $new)
           Set-Content -Path mimir/lit/lit.cfg.py -Value $content -NoNewline
           Select-String -Path mimir/lit/lit.cfg.py -Pattern "FileCheck"

      - name: Install LIT
        shell: cmd
        run: python -m pip install lit psutil

      - name: Install FileCheck
        uses: i3h/download-release-asset@v1
        with:
          owner: fodinabor
          repo: FileCheckBuild
          tag: latest
          file: FileCheck-x86_64-win.exe
          path: mimir/bin
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure
        shell: cmd
        run: |
          call "C:\Program Files\Microsoft Visual Studio\2022\Enterprise\VC\Auxiliary\Build\vcvars64.bat"
          set CC=cl.exe
          set CXX=cl.exe
          python -c "import sys; print(sys.executable)" > tmpPath
          set /p PYTHON_EXE= < tmpPath
          cmake -S mimir -B mimir/build -G Ninja   ^
                -DCMAKE_BUILD_TYPE=${{matrix.build-type}} ^
                -DBUILD_TESTING=ON                        ^
                -DMIM_BUILD_EXAMPLES=ON                   ^
                -DPython3_EXECUTABLE=%PYTHON_EXE%         ^
                -DPython_EXECUTABLE=%PYTHON_EXE%          ^
                -DPYTHON_EXECUTABLE=%PYTHON_EXE%          ^
                -DMIM_LIT_TIMEOUT=300                     ^
                -DMIM_FILECHECK=%cd%/mimir/bin/FileCheck-x86_64-win.exe

      - name: Install LLVM and Clang
        uses: KyleMayes/install-llvm-action@v2.0.9
        with:
          version: "21.1.8"

      - name: Build
        shell: cmd
        run: |
          call "C:\Program Files\Microsoft Visual Studio\2022\Enterprise\VC\Auxiliary\Build\vcvars64.bat"
          cmake --build mimir/build -v
          

      - name: Test
        working-directory: mimir/build
        shell: cmd
        run: |
          call "C:\Program Files\Microsoft Visual Studio\2022\Enterprise\VC\Auxiliary\Build\vcvars64.bat"
          set LIT_USE_INTERNAL_SHELL=0
          set PATH=%GITHUB_WORKSPACE%\mimir\bin;%PATH%
          cmake --build . -v --target test-all
